import {
  ScreenContainer,
  Button,
  HeroSection,
  Hero,
  HeroBody,
  HeroTitle,
  HeroTeaser,
  HeroActionList,
  HeroAction,
} from 'smooth-doc/components';
import heroBackgroundImageURL from '../images/hero-background.png';
import { Helmet } from 'react-helmet';

<HeroSection>
  <Helmet meta={[{ name: 'keywords', content: 'react mask, node mask, react masker, react, mask, javascript mask, jsmask' }]} />
  <Hero backgroundImageURL={heroBackgroundImageURL}>
    <HeroBody>
      <HeroTitle>Node-masker is a javascript mask library made in TypeScript 🎭</HeroTitle>
      <HeroTeaser>Multi-Mask operation and Compatibility with large scale applications using Node and React 👋 ⚛️</HeroTeaser>
      <HeroActionList>
        <HeroAction>
          <Button variant="neutral" forwardedAs="a" href="/docs/">
            Browse Docs
          </Button>
        </HeroAction>
      </HeroActionList>
    </HeroBody>
  </Hero>
</HeroSection>
